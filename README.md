# cleanseObject

Removes undefined and null elements from Objects

## Instructions:

In order to run the code just hit

```sh
npm install
tsc index.ts
node index.js
```

You should be able to see the result printed.
Also, if you want to test you can replace the `data` variable in the .ts code,
just make sure to build it again.
